File: app/src/main/kotlin/eu/hxreborn/biometricapplock/util/RootShell.kt @ line None
User: hxreborn
Body: magisk blocks su while its allow dialog is up and that dialog waits 10s by default (Config.kt suDefaultTimeout) before auto-denying, so a 5s watchdog kills the process mid-prompt and isRootGranted reads false on a fresh install
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/util/BiometricAuthUtil.kt @ line None
User: hxreborn
Body: has this been seen to show face on a Samsung? canAuthenticate(WEAK) said NONE_ENROLLED, so i'd expect BiometricPrompt to fail with ERROR_NO_BIOMETRICS and land on the same cancelled result as before
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/hook/SystemServerHooks.kt @ line None
User: hxreborn
Body: atmsRef is only set on the first intercept, so a locked app posting a notification right after boot evaluates this with ctx null and the lazy pins the english fallback for the rest of the process. there are no values-xx resources yet either, so right now this only adds a PackageManager call. drop the lookup until translations exist?
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/util/DiagnosticsExporter.kt @ line None
User: hxreborn
Body: on Vector the hook lines reach neither file: after a boot with the module active both hold 0 hook lines, the one match is Vector's own Loading class line, present in both. the logcat command below is what already captures them. where the files do carry the lines, verbose on means each one exports twice
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/ui/component/BiometricHardwareCard.kt @ line None
User: hxreborn
Body: true at both call sites now, so the param can go
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/util/FaceScanOverlay.kt @ line None
User: hxreborn
Body: scanWidth comes from pulsedHalf, which bracketPulse changes every frame, so this still allocates a gradient per draw. build it once over halfSize * 1.4f and put the pulse scale in the matrix
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/hook/SystemServerHooks.kt @ line 225
User: hxreborn
Body: this rewrite is discarded. ActivityStarter only reads the interceptor's fields when intercept returns true (ActivityStarter.java:1151), and it never copies mUserId at all, only intent, rInfo, aInfo, callingPid and callingUid, so the record is still created for user 0 whatever we set here. on device the resume prompt now shows, but after auth `top resumed pkg=eu.hxreborn.notiftest user=0` and the personal copy is on screen. it also passes auth.callingUid, which is -1 for tokens minted by postAuthLaunch. the launcher path already solves this: a stashed launch goes through resumeOriginalLaunch, which is startActivityAsUser from uid 1000. stashing a launcher intent for entry.packageName in postAuthLaunch would route this case through that same path and the block here can go
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/util/FaceScanOverlay.kt @ line 261
User: hxreborn
Body: bracketPulse runs 0..1 (ofFloat(0f, 1f) in startPulseAnimation), so this scales the gradient down to zero width at the low end of the pulse. the factor wanted is pulseScale, 1f + 0.05f * bracketPulse
----------------------------------------
File: app/src/main/kotlin/eu/hxreborn/biometricapplock/util/DiagnosticsExporter.kt @ line 24
User: hxreborn
Body: my mistake, the earlier note only holds on Vector. on stock LSPosed 2.0.4 the hook lines are tagged LSPosedFramework with the module tag inside the message, so this `-s BiometricAppLock` filter gets 0 of them, while modules_*.log and verbose_*.log both hold them (27 each after a boot). so the file grep has to come back. modules_*.log alone is enough: it carries every hook line on stock LSPosed without the verbose duplicate, and on Vector it's empty either way
----------------------------------------
File: app/src/main/res/values/strings.xml @ line 48
User: hxreborn
Body: these two are unused now that the lookup is gone
----------------------------------------
